# Archived segmentation validation guide

The segmentation experiments now run through the strict modular pipeline.
Use [pipeline-configuration.md](pipeline-configuration.md) for the three
segmentation methods, atomic split modes, ten-configuration matrix, and cloud
validation commands.
